services:
  consul:
    image: hashicorp/consul:1.17
    command:
      - agent
      - -dev
      - -client=0.0.0.0
      - -hcl
      - telemetry { prometheus_retention_time = "1m", disable_hostname = true }
    ports:
      - "8500:8500"
